_format_version: "2.1"
services:
  - name: anything
    url: https://httpbin.org/anything
    routes:
      - name: anything
        paths: [ '/anything' ]
    plugins:
      - name: js_hello
        config:
          message: javascript_hello
      - name: ts_hello
        config:
          message: typescript_hello
